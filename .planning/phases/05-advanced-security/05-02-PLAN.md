---
phase: 05-advanced-security
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [services/security.service.ts, services/withdrawal.service.ts, app/api/withdrawal/route.ts, components/dashboard/WithdrawalModal.tsx, components/security/TwoFactorChallenge.tsx, tests/e2e/withdrawal-2fa.spec.ts]
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "User must provide a TOTP code or Backup Code to initiate a withdrawal if 2FA is enabled"
    - "Withdrawals are blocked if 2FA is enabled but no valid code is provided"
  artifacts:
    - path: "components/security/TwoFactorChallenge.tsx"
      provides: "Reusable 2FA/Backup Code input component"
    - path: "tests/e2e/withdrawal-2fa.spec.ts"
      provides: "Automated verification of the protected flow"
  key_links:
    - from: "services/withdrawal.service.ts"
      to: "services/security.service.ts"
      via: "verifying security code (TOTP or Backup) before debiting ledger"
---

<objective>
Integrate 2FA verification into the withdrawal flow, supporting both TOTP and Backup codes, and verify the implementation with automated E2E tests.

Purpose: Prevent unauthorized fund movement and ensure high-fidelity verification of security guards.
Output: Secure withdrawal process and Playwright test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-advanced-security/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Security Service for Multi-Code Verification</name>
  <files>services/security.service.ts</files>
  <action>
    - Implement `verifySecurityCode(userId, code)` in `security.service.ts`.
    - This method should first try verifying `code` as a TOTP token (using decrypted `totpSecret`).
    - If TOTP fails, it should attempt to verify `code` as a Backup Code (check `twoFactorBackupCodes` array, remove on success).
    - Return a boolean or throw if both fail.
  </action>
  <verify>
    <automated>npm test services/security.service.ts (unit tests for multi-code verify)</automated>
  </verify>
  <done>Security service supports both TOTP and Backup code verification.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 2FA Challenge UI and Update Withdrawal Flow</name>
  <files>components/security/TwoFactorChallenge.tsx, components/dashboard/WithdrawalModal.tsx, services/withdrawal.service.ts, app/api/withdrawal/route.ts</files>
  <action>
    - Create `TwoFactorChallenge` component: PIN input field that accepts 6-8 characters (supporting 6-digit TOTP and 8-char backup codes).
    - Update `WithdrawalModal` to show the challenge step if `user.totpEnabled` is true.
    - Update `withdrawal.service.ts` to call `securityService.verifySecurityCode` before processing the withdrawal.
    - Update the API route to pass the `securityCode` from the request body.
  </action>
  <verify>
    <automated>npx vitest components/security/TwoFactorChallenge.test.tsx</automated>
  </verify>
  <done>Withdrawal UI and Service are protected by 2FA.</done>
</task>

<task type="auto">
  <name>Task 3: Create Playwright E2E Tests for Withdrawal 2FA</name>
  <files>tests/e2e/withdrawal-2fa.spec.ts</files>
  <action>
    - Create an E2E test that:
      - Authenticates as a user with 2FA enabled.
      - Attempts to withdraw without a code (should fail/show prompt).
      - Attempts to withdraw with an invalid code (should fail).
      - Attempts to withdraw with a valid TOTP code (mocked or generated via `otplib` in test).
      - Verifies the withdrawal is successfully submitted.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/withdrawal-2fa.spec.ts</automated>
  </verify>
  <done>Withdrawal 2FA flow is verified by automated E2E tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Withdrawal UI | 2FA/Backup code entered |
| Withdrawal UI → API | Code sent in withdrawal payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | Withdrawal Action | mitigate | Enforce 2FA verification in `withdrawal.service.ts` before any state changes |
| T-05-05 | Information Disclosure | Backup Codes | mitigate | One-time use for backup codes; verify against hashed/encrypted storage |
</threat_model>

<verification>
- Withdrawal service unit tests pass.
- Playwright E2E tests pass for the entire flow.
</verification>

<success_criteria>
- Withdrawals are protected by 2FA (TOTP + Backup Codes).
- Automated E2E verification is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/05-advanced-security/05-02-SUMMARY.md`
</output>
