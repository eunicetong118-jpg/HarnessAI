---
phase: 05-advanced-security
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [services/security.service.ts, components/dashboard/settings/BackupCodes.tsx, auth.ts, app/auth/2fa/page.tsx, next.config.mjs, middleware.ts]
autonomous: true
requirements: [AUTH-06, SEC-01]

must_haves:
  truths:
    - "User is prompted for 2FA during login if enabled"
    - "User can use a Backup Code to log in if they lose their TOTP device"
    - "Application serves strict security headers (CSP, HSTS)"
  artifacts:
    - path: "app/auth/2fa/page.tsx"
      provides: "Dedicated 2FA challenge page for login flow"
    - path: "components/dashboard/settings/BackupCodes.tsx"
      provides: "UI to generate and download recovery codes"
  key_links:
    - from: "auth.ts"
      to: "app/auth/2fa/page.tsx"
      via: "redirecting users with 2FA enabled to the challenge page"
---

<objective>
Implement recovery mechanisms (Backup Codes), integrate 2FA into the NextAuth login flow, and harden the application infrastructure.

Purpose: Ensure users aren't locked out of their accounts and prevent unauthorized access at the entry point.
Output: Full 2FA login flow and recovery UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-advanced-security/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Backup Codes Generation and UI</name>
  <files>services/security.service.ts, components/dashboard/settings/BackupCodes.tsx</files>
  <action>
    - Implement `generateBackupCodes(userId)` in `security.service.ts`:
      - Generate 10 random 8-character alphanumeric codes.
      - Hash them with `bcryptjs` and store in `twoFactorBackupCodes`.
    - Create `BackupCodes.tsx` component for the settings page.
    - Users can view and download their codes (only shown once or downloadable as TXT).
  </action>
  <verify>
    <automated>npx vitest run services/security.service.ts</automated>
  </verify>
  <done>Users can generate and manage recovery backup codes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate 2FA into NextAuth Login Flow</name>
  <files>auth.ts, app/auth/2fa/page.tsx, middleware.ts</files>
  <action>
    - Update `auth.ts` (NextAuth v5 configuration):
      - In `signIn` callback: if user has `totpEnabled: true`, set a temporary flag in the session or JWT, but do not fully authenticate yet (or redirect to `/auth/2fa`).
      - Create `/auth/2fa` page using the `TwoFactorChallenge` component.
      - This page verifies the code via a Server Action or API and then completes the login (e.g. by setting a `2fa_verified` cookie or updating the session).
    - Update `middleware.ts` to block access to protected routes if 2FA is required but not verified.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/login-2fa.spec.ts</automated>
  </verify>
  <done>Login flow requires 2FA for enrolled users.</done>
</task>

<task type="auto">
  <name>Task 3: Harden Security Headers and CSP</name>
  <files>next.config.mjs, middleware.ts</files>
  <action>
    - Configure `next.config.mjs` with:
      - `X-Frame-Options: DENY`
      - `X-Content-Type-Options: nosniff`
      - `Referrer-Policy: strict-origin-when-cross-origin`
      - `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
    - Implement a Content Security Policy (CSP) in `middleware.ts` (Nonce-based if possible) to prevent XSS.
  </action>
  <verify>
    <automated>curl -I http://localhost:3000</automated>
  </verify>
  <done>Application infrastructure is hardened with security headers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Login Flow | Multi-step authentication boundary |
| Server → Browser | Security headers enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Spoofing | Login | mitigate | Require 2FA verification before granting full session access |
| T-05-07 | Information Disclosure | Backup Codes | mitigate | Hash backup codes in DB; one-time use |
| T-05-08 | Tampering | Browser | mitigate | CSP to prevent malicious script execution |
</threat_model>

<verification>
- User with 2FA enabled is redirected to /auth/2fa after entering password.
- User can log in using a valid Backup Code.
- Security headers are present in production build.
</verification>

<success_criteria>
- 2FA integrated into Login.
- Backup codes functional for recovery.
- Infrastructure hardened.
</success_criteria>

<output>
After completion, create `.planning/phases/05-advanced-security/05-03-SUMMARY.md`
</output>
