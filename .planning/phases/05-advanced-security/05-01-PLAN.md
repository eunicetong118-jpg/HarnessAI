---
phase: 05-advanced-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, lib/security/crypto.ts, lib/totp.ts, services/security.service.ts, app/api/auth/2fa/enroll/route.ts, app/api/auth/2fa/verify/route.ts, components/dashboard/settings/TwoFactorEnrollment.tsx, .planning/phases/05-advanced-security/05-VALIDATION.md]
autonomous: true
requirements: [AUTH-06]
user_setup:
  - service: Encryption
    why: "Secure storage of 2FA secrets"
    env_vars:
      - name: ENCRYPTION_KEY
        source: "Generate a 32-byte (64-char) hex string"

must_haves:
  truths:
    - "User can generate a 2FA secret and see a QR code"
    - "User can verify a TOTP code to enable 2FA"
    - "2FA secrets are encrypted in the database immediately upon enrollment start"
  artifacts:
    - path: "lib/security/crypto.ts"
      provides: "AES-256-GCM encryption/decryption"
    - path: "lib/totp.ts"
      provides: "TOTP generation and verification logic using otplib"
    - path: "services/security.service.ts"
      provides: "Enrollment business logic (D-04: save secret with totpEnabled: false)"
  key_links:
    - from: "services/security.service.ts"
      to: "lib/security/crypto.ts"
      via: "encrypting secrets before DB storage"
---

<objective>
Implement the core TOTP 2FA infrastructure and enrollment flow, ensuring secrets are encrypted at rest from the moment they are generated.

Purpose: Provide a baseline for advanced account security and satisfy "Secure TOTP secret storage" requirement.
Output: Working 2FA enrollment with encrypted storage and a validation template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-advanced-security/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Validation Template and Update Schema</name>
  <files>prisma/schema.prisma, .planning/phases/05-advanced-security/05-VALIDATION.md</files>
  <action>
    - Add `twoFactorBackupCodes String[]` to the `User` model in `prisma/schema.prisma`.
    - Run `npx prisma generate`.
    - Create `.planning/phases/05-advanced-security/05-VALIDATION.md` with sections for "TOTP Enrollment", "Withdrawal Guard", "Login 2FA", "Backup Codes", and "Infrastructure Hardening".
  </action>
  <verify>
    <automated>ls .planning/phases/05-advanced-security/05-VALIDATION.md && grep "twoFactorBackupCodes" prisma/schema.prisma</automated>
  </verify>
  <done>Schema updated and validation document initialized.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Crypto Utils and TOTP Logic</name>
  <files>lib/security/crypto.ts, lib/totp.ts</files>
  <action>
    - Install `qrcode`, `@types/qrcode`, and `otplib`.
    - Create `lib/security/crypto.ts` implementing AES-256-GCM encryption/decryption.
      - Use `process.env.ENCRYPTION_KEY` (32-byte hex).
      - Output format: `iv:tag:encryptedData`.
    - Create `lib/totp.ts` as a wrapper for `otplib` (authenticator).
      - Implement `generateSecret()`, `generateKeyuri(email, secret)`, and `verifyToken(secret, token)`.
  </action>
  <verify>
    <automated>npx vitest lib/security/crypto.ts lib/totp.ts (mocking env for tests)</automated>
  </verify>
  <done>Cryptographic and TOTP primitives implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Security Service and Enrollment API</name>
  <files>services/security.service.ts, app/api/auth/2fa/enroll/route.ts, app/api/auth/2fa/verify/route.ts, components/dashboard/settings/TwoFactorEnrollment.tsx</files>
  <action>
    - Implement `services/security.service.ts`:
      - `generateSetup(userId)`: Generate secret, ENCRYPT it, and SAVE to `User.totpSecret` immediately with `totpEnabled: false`. Generate and return QR DataURL.
      - `verifyAndEnable(userId, token)`: Decrypt `totpSecret`, verify token, set `totpEnabled: true`.
    - Create API routes for enrollment and verification.
    - Implement `TwoFactorEnrollment` UI component in the settings page.
  </action>
  <verify>
    <automated>npm test services/security.service.ts</automated>
  </verify>
  <done>2FA enrollment flow functional with secure immediate storage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | QR data sent to client; TOTP tokens received from client |
| API → Database | Encrypted secrets stored in DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Database | mitigate | AES-256-GCM encryption for all TOTP secrets |
| T-05-02 | Tampering | Enrollment Flow | mitigate | Verify TOTP token before enabling `totpEnabled` flag |
| T-05-03 | Spoofing | QR Generation | mitigate | Generate QR DataURL on server; never expose raw secret to client |
</threat_model>

<verification>
- User can start 2FA enrollment and see a QR code.
- Checking DB shows `totpSecret` populated and encrypted even before verification.
- User can successfully enable 2FA with a valid code.
</verification>

<success_criteria>
- TOTP Enrollment flow is complete.
- Secrets are never stored in plaintext.
- `05-VALIDATION.md` exists.
</success_criteria>

<output>
After completion, create `.planning/phases/05-advanced-security/05-01-SUMMARY.md`
</output>
