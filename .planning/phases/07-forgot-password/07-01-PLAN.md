---
phase: 07-forgot-password
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [services/auth.service.ts, app/api/auth/forgot-password/route.ts, app/api/auth/reset-password/route.ts]
autonomous: true
requirements: [AUTH-09]

must_haves:
  truths:
    - "Password reset tokens are generated and stored with 1h expiry"
    - "Email with reset link is triggered for valid accounts"
    - "Passwords can be updated using valid tokens"
    - "2FA is disabled upon successful password reset"
  artifacts:
    - path: "services/auth.service.ts"
      provides: "Password reset logic"
    - path: "app/api/auth/forgot-password/route.ts"
      provides: "Reset request endpoint"
    - path: "app/api/auth/reset-password/route.ts"
      provides: "Password update endpoint"
  key_links:
    - from: "services/auth.service.ts"
      to: "services/actionToken.service.ts"
      via: "token generation/validation"
---

<objective>
Implement the backend logic for password reset including token generation, email triggering, and password updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@services/actionToken.service.ts
@services/emailVerification.service.ts
@lib/email.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Create AuthService and password reset methods</name>
  <files>services/auth.service.ts</files>
  <action>
    Create AuthService with two static methods:
    1. `requestPasswordReset(email: string)`: Finds user, creates PASSWORD_RESET token (using ActionTokenService), and sends email (using lib/email).
    2. `resetPassword(token: string, userId: string, newPassword: string)`: Validates token, hashes new password (bcryptjs), updates User (password, totpEnabled: false), and consumes token.
  </action>
  <verify>
    <automated>npx vitest services/auth.service.test.ts</automated>
  </verify>
  <done>Business logic for password reset implemented and tested.</done>
</task>

<task type="auto">
  <name>Implement forgot-password API route</name>
  <files>app/api/auth/forgot-password/route.ts</files>
  <action>
    Create POST handler that:
    - Validates email input (zod).
    - Calls `AuthService.requestPasswordReset`.
    - Returns 200 regardless of whether user exists (security best practice to prevent enumeration).
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/auth/forgot-password -d '{"email":"test@example.com"}' -H "Content-Type: application/json"</automated>
  </verify>
  <done>Endpoint for requesting password reset exists.</done>
</task>

<task type="auto">
  <name>Implement reset-password API route</name>
  <files>app/api/auth/reset-password/route.ts</files>
  <action>
    Create POST handler that:
    - Accepts token, userId, and password.
    - Calls `AuthService.resetPassword`.
    - Returns success or error message.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/auth/reset-password -d '{"token":"...", "userId":"...", "password":"..."}' -H "Content-Type: application/json"</automated>
  </verify>
  <done>Endpoint for resetting password exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted input (email, token, password) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | forgot-password route | mitigate | Return 200 for all requests to prevent email enumeration |
| T-07-02 | Tampering | reset-password route | mitigate | Validate token expiry and consumption status |
| T-07-03 | Elevation of Privilege | reset-password route | mitigate | Ensure token corresponds to the userId being reset |
</threat_model>

<success_criteria>
Backend support for password reset is fully functional and secure.
</success_criteria>

<output>
After completion, create `.planning/phases/07-forgot-password/07-01-SUMMARY.md`
</output>
