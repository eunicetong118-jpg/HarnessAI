---
phase: 07-forgot-password
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [app/(auth)/forgot-password/page.tsx, app/(auth)/reset-password/[token]/page.tsx]
autonomous: true
requirements: [AUTH-09]

must_haves:
  truths:
    - "Forgot password page allows email submission"
    - "Reset password page handles token from URL"
    - "UI matches dark theme and existing auth pages"
  artifacts:
    - path: "app/(auth)/forgot-password/page.tsx"
      provides: "Forgot password form"
    - path: "app/(auth)/reset-password/[token]/page.tsx"
      provides: "Reset password form"
---

<objective>
Implement the frontend UI for requesting a password reset and performing the reset using a token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@app/(auth)/login/page.tsx
@app/(auth)/signup/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Create Forgot Password Page</name>
  <files>app/(auth)/forgot-password/page.tsx</files>
  <action>
    Implement forgot password page:
    - UI: Use the same glassmorphism style as Login/Signup.
    - Form: Email input.
    - Behavior: POST to /api/auth/forgot-password. Show success message even if email doesn't exist.
  </action>
  <verify>
    <automated>ls app/\(auth\)/forgot-password/page.tsx</automated>
  </verify>
  <done>Forgot password UI implemented.</done>
</task>

<task type="auto">
  <name>Create Reset Password Page</name>
  <files>app/(auth)/reset-password/[token]/page.tsx</files>
  <action>
    Implement reset password page:
    - UI: Dark theme glassmorphism.
    - Form: New password, Confirm password.
    - Behavior: Extract token from URL, extract userId from searchParams (if provided in link), POST to /api/auth/reset-password.
  </action>
  <verify>
    <automated>ls app/\(auth\)/reset-password/\[token\]/page.tsx</automated>
  </verify>
  <done>Reset password UI implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Password reset frontend flow</what-built>
  <how-to-verify>
    1. Navigate to /login and click "Forgot password?".
    2. Enter your email and submit.
    3. Verify the "Email sent" (stubbed in console) success message.
    4. Manually navigate to /reset-password/[some-token] and verify the UI looks correct.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
User can complete the full password reset flow from the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/07-forgot-password/07-02-SUMMARY.md`
</output>
