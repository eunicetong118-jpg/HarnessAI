---
phase: "04-admin-financials"
plan: "04"
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: [
  "app/(protected)/admin/users/page.tsx",
  "services/admin.service.ts",
  "components/admin/UserTable.tsx"
]
autonomous: true
requirements: ["ADMIN-02"]

must_haves:
  truths:
    - "Admin can view all users and their account statuses"
    - "Admin can disable or enable any user account"
    - "Disabled users are blocked from the portal"
  artifacts:
    - path: "app/(protected)/admin/users/page.tsx"
      provides: "User management dashboard"
    - path: "services/admin.service.ts"
      provides: "User administration logic"
---

<objective>
Implement administrative user management, allowing admins to oversee user activity, manage account status (enable/disable), and monitor balances.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-financials/04-RESEARCH.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AdminService for User Management</name>
  <files>services/admin.service.ts, services/admin.service.test.ts</files>
  <behavior>
    - getUsers:
      - Returns list of users with their role, isDisabled status, email verification status, and balance summary.
    - toggleUserDisabled:
      - Flips isDisabled flag for a user.
    - triggerManualVerification:
      - Manually sets a user's BrokerAccount to VERIFIED.
  </behavior>
  <action>
    Implement AdminService to handle bulk user queries and individual status updates.
    Ensure balances are calculated accurately using LedgerService integration.
  </action>
  <verify>
    <automated>npx vitest services/admin.service.test.ts</automated>
  </verify>
  <done>AdminService implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Build User Management UI</name>
  <files>app/(protected)/admin/users/page.tsx, components/admin/UserTable.tsx</files>
  <action>
    - Create UserTable component:
      - Columns: Name/Email, Role, Balance, Account Status, Actions.
      - "Disable" / "Enable" toggle button.
      - "Verify MT5" manual trigger button.
    - Implement search/filter by email or status.
    - Integrate with AdminService via Server Actions or API routes.
  </action>
  <verify>
    - Toggle Disable for a user -> User status updates in DB.
    - Try to login as disabled user -> Middleware blocks and redirects to login with error.
  </verify>
  <done>User management interface functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin → User Account | Admin can take destructive actions (disabling accounts) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Elevation of Privilege | User Management API | mitigate | Strict ADMIN role check on all AdminService methods |
| T-04-04-02 | Tampering | User Status | mitigate | Server-side validation of toggle actions |
</threat_model>

<verification>
- User table accurately displays user information and balances.
- The isDisabled flag correctly blocks portal access (verified by middleware).
</verification>

<success_criteria>
- Admin can successfully manage user account states.
- User management UI is responsive and follows design tokens.
</success_criteria>

<output>
After completion, create .planning/phases/04-admin-financials/04-04-SUMMARY.md
</output>
