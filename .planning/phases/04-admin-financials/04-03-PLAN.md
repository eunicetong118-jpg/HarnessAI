---
phase: "04-admin-financials"
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [
  "app/(protected)/admin/tickets/page.tsx",
  "components/admin/TicketTable.tsx",
  "components/admin/TicketActions.tsx",
  "app/api/admin/verify-accounts/route.ts",
  "components/admin/BulkVerifyDialog.tsx",
  "services/verification.service.ts"
]
autonomous: true
requirements: ["ADMIN-02"]

must_haves:
  truths:
    - "Admin can view pending verification and withdrawal tickets"
    - "Admin can claim a ticket to show ownership"
    - "Admin can resolve a ticket, updating the user's status"
    - "Admin can bulk verify MT5 accounts via CSV upload"
  artifacts:
    - path: "app/(protected)/admin/tickets/page.tsx"
      provides: "Unified ticket processing interface"
    - path: "services/verification.service.ts"
      provides: "Bulk verification logic"
---

<objective>
Implement the Ticket Command Center and the Bulk MT5 Verification system for administrative operations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-financials/04-RESEARCH.md
@services/ticket.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Ticket Command Center UI</name>
  <files>app/(protected)/admin/tickets/page.tsx, components/admin/TicketTable.tsx</files>
  <action>
    - Create a tabbed interface (Verifications | Withdrawals).
    - Implement a TicketTable component:
      - Columns: Date, User, Details (Amount/MT5#), Status, Assignee, Actions.
    - Fetch data using server-side queries via TicketService.
  </action>
  <verify>
    - View Tickets page -> Shows list of tickets.
  </verify>
  <done>Ticket list UI functional.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Ticket Actions (Claim/Complete)</name>
  <files>components/admin/TicketActions.tsx, app/api/admin/tickets/[id]/route.ts</files>
  <action>
    - Create TicketActions component with Claim/Complete buttons.
    - Implement API routes for PATCH /api/admin/tickets/[id]/claim and /resolve.
    - Resolve logic handles BrokerAccount status flip for Verifications.
  </action>
  <verify>
    - Click Complete (Verification) -> User account becomes VERIFIED.
  </verify>
  <done>Admins can process tickets from the UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Bulk MT5 Verification API & UI</name>
  <files>services/verification.service.ts, app/api/admin/verify-accounts/route.ts, components/admin/BulkVerifyDialog.tsx</files>
  <action>
    - Implement verification.service.ts:
      - parseCsv(stream): Extracts mt5AccountNo values.
      - bulkVerify(mt5Numbers): Flips matching BrokerAccounts to VERIFIED, closes associated Tickets, queues notifications.
    - Create POST /api/admin/verify-accounts route.
    - Create BulkVerifyDialog in UI to handle CSV upload and display results summary (verified, notFound, alreadyVerified).
    - Adhere to Section 8 of technical spec for logic and response format.
  </action>
  <verify>
    - Upload CSV with 2 pending MT5 numbers -> API returns verified: 2, accounts are VERIFIED.
  </verify>
  <done>Bulk MT5 verification functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin → Database | Ticket resolution and bulk verification modify core user state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Ticket Resolution | mitigate | Server-side validation that only claimed tickets can be resolved |
| T-04-03-02 | Repudiation | Ticket Actions | mitigate | Audit trail via Ticket.assigneeUserId and Ticket.closedAt |
| T-04-03-03 | Tampering | Bulk Verification | mitigate | Enforce ADMIN role on CSV processing endpoint |
</threat_model>

<verification>
- Tickets move from PENDING to DONE status correctly.
- Bulk verification successfully flips multiple BrokerAccount statuses and closes tickets.
</verification>

<success_criteria>
- Admins can process tickets end-to-end.
- Bulk verification API correctly reports counts and updates DB.
</success_criteria>

<output>
After completion, create .planning/phases/04-admin-financials/04-03-SUMMARY.md
</output>
