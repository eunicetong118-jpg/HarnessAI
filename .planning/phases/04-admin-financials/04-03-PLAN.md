---
phase: "04-admin-financials"
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [
  "app/(protected)/admin/tickets/page.tsx",
  "components/admin/TicketTable.tsx",
  "components/admin/TicketActions.tsx"
]
autonomous: true
requirements: ["ADMIN-02"]

must_haves:
  truths:
    - "Admin can view pending verification and withdrawal tickets"
    - "Admin can claim a ticket to show ownership"
    - "Admin can resolve a ticket, updating the user's status"
  artifacts:
    - path: "app/(protected)/admin/tickets/page.tsx"
      provides: "Unified ticket processing interface"
---

<objective>
Implement the Ticket Command Center where admins can process user requests for account verification and fund withdrawals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-financials/04-RESEARCH.md
@services/ticket.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Ticket Command Center UI</name>
  <files>app/(protected)/admin/tickets/page.tsx, components/admin/TicketTable.tsx</files>
  <action>
    - Create a tabbed interface (Verifications | Withdrawals).
    - Implement a TicketTable component:
      - Columns: Date, User, Details (Amount/MT5#), Status, Assignee, Actions.
      - Empty state design.
      - Loading skeletons.
    - Fetch data using server-side queries via TicketService.
  </action>
  <verify>
    - View Tickets page -> Shows list of tickets.
    - Switch tabs -> Filters tickets by type.
  </verify>
  <done>Ticket list UI functional.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Ticket Actions (Claim/Complete)</name>
  <files>components/admin/TicketActions.tsx, app/api/admin/tickets/[id]/route.ts</files>
  <action>
    - Create TicketActions component with:
      - "Claim" button (visible if unassigned).
      - "Complete" button (visible if claimed by current admin).
    - Implement API routes/Server Actions for these actions:
      - PATCH /api/admin/tickets/[id]/claim
      - PATCH /api/admin/tickets/[id]/resolve
    - Resolve logic:
      - For VERIFICATION: Update BrokerAccount.status to VERIFIED.
      - For WITHDRAWAL: Update Ticket.status to DONE.
    - Trigger in-app notification upon resolution.
  </action>
  <verify>
    - Click Claim -> Assignee updates.
    - Click Complete (Verification) -> Ticket becomes DONE, User account becomes VERIFIED.
    - Click Complete (Withdrawal) -> Ticket becomes DONE.
  </verify>
  <done>Admins can process tickets from the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin → Database | Ticket resolution modifies core user financial and verification state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Ticket Resolution | mitigate | Server-side validation that only claimed tickets can be resolved |
| T-04-03-02 | Repudiation | Ticket Actions | mitigate | Audit trail via Ticket.assigneeUserId and Ticket.closedAt |
</threat_model>

<verification>
- Tickets move from PENDING to DONE status correctly.
- Verification tickets successfully flip BrokerAccount status.
</verification>

<success_criteria>
- Admins can process tickets end-to-end.
- User status updates automatically upon ticket resolution.
</success_criteria>

<output>
After completion, create .planning/phases/04-admin-financials/04-03-SUMMARY.md
</output>
