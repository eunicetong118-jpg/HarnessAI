---
phase: "04-admin-financials"
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [
  "app/(protected)/admin/layout.tsx",
  "app/(protected)/admin/page.tsx",
  "services/ticket.service.ts"
]
autonomous: true
requirements: ["ADMIN-01"]

must_haves:
  truths:
    - "Only users with role ADMIN can access /admin routes"
    - "Admin layout provides navigation to tickets and users"
  artifacts:
    - path: "app/(protected)/admin/layout.tsx"
      provides: "Role-based access control and navigation"
    - path: "services/ticket.service.ts"
      provides: "Ticket management logic for admins"
---

<objective>
Setup the administrative foundation, including role-based security and the service for managing support tickets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-financials/04-RESEARCH.md
@prisma/schema.prisma
@lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Admin Layout and Protection</name>
  <files>app/(protected)/admin/layout.tsx, app/(protected)/admin/page.tsx</files>
  <action>
    - Create app/(protected)/admin/layout.tsx:
      - Check session.user.role. If not ADMIN, redirect to /dashboard.
      - Provide a sidebar/navigation for "Tickets" and "Users".
      - Follow the premium dark theme specified in RESEARCH.md.
    - Create app/(protected)/admin/page.tsx:
      - Redirect to /admin/tickets by default.
  </action>
  <verify>
    - Access /admin as a USER -> Redirect to /dashboard.
    - Access /admin as an ADMIN -> Show admin layout.
  </verify>
  <done>Admin area secured and layout created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TicketService for Admins</name>
  <files>services/ticket.service.ts, services/ticket.service.test.ts</files>
  <behavior>
    - getTickets:
      - Filters by status (PENDING/DONE).
      - Filters by type (WITHDRAWAL/VERIFICATION).
      - Includes user details (name, email).
    - claimTicket:
      - Sets assigneeUserId to current admin ID.
    - resolveTicket:
      - Sets status to DONE.
      - Sets closedAt to now.
      - If VERIFICATION: sets associated BrokerAccount status to VERIFIED (if metadata includes account info).
  </behavior>
  <action>
    Implement TicketService with methods required for administrative processing.
    Ensure data integrity when updating ticket and account statuses.
  </action>
  <verify>
    <automated>npx vitest services/ticket.service.test.ts</automated>
  </verify>
  <done>TicketService implemented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → Admin API | Admin actions require strict role verification |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Elevation of Privilege | Admin Routes | mitigate | Middleware and Layout-level role checks (role === ADMIN) |
| T-04-02-02 | Information Disclosure | Ticket Data | mitigate | Role-based access control on TicketService methods |
</threat_model>

<verification>
- Admin routes are unreachable for non-admins.
- TicketService can correctly claim and resolve tickets.
</verification>

<success_criteria>
- Admin layout correctly redirects non-admins.
- TicketService passes all unit tests.
</success_criteria>

<output>
After completion, create .planning/phases/04-admin-financials/04-02-SUMMARY.md
</output>
