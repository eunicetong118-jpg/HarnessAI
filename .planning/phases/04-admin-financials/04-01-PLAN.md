---
phase: "04-admin-financials"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "services/withdrawal.service.ts",
  "app/(protected)/dashboard/withdraw/page.tsx",
  "app/api/withdrawal/route.ts"
]
autonomous: true
requirements: ["FIN-01", "FIN-02"]

must_haves:
  truths:
    - "User can request a withdrawal via a ticket system"
    - "Withdrawal amount is debited from available balance immediately"
    - "User can view withdrawal history"
  artifacts:
    - path: "services/withdrawal.service.ts"
      provides: "Transactional withdrawal logic"
    - path: "app/(protected)/dashboard/withdraw/page.tsx"
      provides: "Withdrawal request interface"
  key_links:
    - from: "services/withdrawal.service.ts"
      to: "prisma.$transaction"
      via: "atomic balance check and debit"
---

<objective>
Implement the core withdrawal logic and the user-facing withdrawal request UI.
This ensures users can spend their earned rebates, with immediate ledger impact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-admin-financials/04-RESEARCH.md
@services/ledger.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WithdrawalService</name>
  <files>services/withdrawal.service.ts, services/withdrawal.service.test.ts</files>
  <behavior>
    - createWithdrawal:
      - Validates amount > 0.
      - Validates amount <= available balance (using getBalance).
      - Executes in a prisma.$transaction.
      - Creates a Ticket (type: WITHDRAWAL, status: PENDING).
      - Creates a Ledger entry (type: DEBIT, category: WITHDRAWAL, referenceId: ticket.id).
      - Returns the created ticket.
    - getWithdrawalHistory:
      - Returns Ticket history filtered by type: WITHDRAWAL for a specific user.
  </behavior>
  <action>
    Implement WithdrawalService using BigInt for all cent calculations.
    Ensure the withdrawal flow is atomic to prevent race conditions.
  </action>
  <verify>
    <automated>npx vitest services/withdrawal.service.test.ts</automated>
  </verify>
  <done>WithdrawalService implemented with tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Withdrawal API and UI</name>
  <files>app/api/withdrawal/route.ts, app/(protected)/dashboard/withdraw/page.tsx</files>
  <action>
    - Create a POST route in app/api/withdrawal/route.ts that calls WithdrawalService.createWithdrawal.
    - Create app/(protected)/dashboard/withdraw/page.tsx with:
      - Balance display (available balance in USD).
      - Input field for withdrawal amount.
      - 2FA check placeholder (if 2FA not enabled, show warning/link to settings).
      - Submit button that triggers the API call.
      - Success/Error toast notifications.
    - Follow Shadcn/ui patterns and Tailwind design tokens from RESEARCH.md.
  </action>
  <verify>
    - Submit a withdrawal request with insufficient funds -> Error toast.
    - Submit a withdrawal request with valid funds -> Success toast, balance decreases.
  </verify>
  <done>Withdrawal UI and API functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Withdrawal amount and destination account are untrusted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Withdrawal API | mitigate | Server-side balance validation within a database transaction |
| T-04-01-02 | Spoofing | Withdrawal API | mitigate | Enforce authenticated session via NextAuth |
| T-04-01-03 | Tampering | Ledger | mitigate | Atomic creation of Ticket and Ledger entry |
</threat_model>

<verification>
- User balance reflects the DEBIT immediately after request.
- A PENDING ticket of type WITHDRAWAL exists in the database.
</verification>

<success_criteria>
- WithdrawalService passing unit tests.
- UI allows creating withdrawal requests.
- Ledger entries correctly reflect withdrawal debits.
</success_criteria>

<output>
After completion, create .planning/phases/04-admin-financials/04-01-SUMMARY.md
</output>
