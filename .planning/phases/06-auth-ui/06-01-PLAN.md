---
phase: 06-auth-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/(auth)/login/page.tsx, app/(auth)/signup/page.tsx, app/(auth)/layout.tsx]
autonomous: true
requirements: [AUTH-07, AUTH-08]
must_haves:
  truths:
    - "Login page renders with email and password fields"
    - "Signup page renders with name, email, and password fields"
    - "Both pages use the dark theme aesthetic (#0A0A0F background, #12121A cards)"
    - "Validation errors are displayed for empty or invalid inputs"
  artifacts:
    - path: "app/(auth)/login/page.tsx"
      provides: "User login interface"
    - path: "app/(auth)/signup/page.tsx"
      provides: "User registration interface"
    - path: "app/(auth)/layout.tsx"
      provides: "Centering and theme styling for auth pages"
---

<objective>
Implement the Login and Signup pages using shadcn/ui components with a dark theme aesthetic.

Purpose: Provide a user-friendly interface for authentication that integrates with NextAuth and the registration API.
Output: app/(auth)/login/page.tsx, app/(auth)/signup/page.tsx, and app/(auth)/layout.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/api/auth/register/route.ts
@lib/auth.ts
@middleware.ts
@components/ui/card.tsx
@components/ui/button.tsx
@components/ui/input.tsx
@components/ui/label.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Auth Layout</name>
  <files>app/(auth)/layout.tsx</files>
  <action>
    Create a layout for the (auth) group that:
    - Centers content vertically and horizontally.
    - Sets the background color to #0A0A0F.
    - Uses a dark theme container.
  </action>
  <verify>
    <automated>grep "#0A0A0F" app/(auth)/layout.tsx</automated>
  </verify>
  <done>Auth layout created with dark theme background</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Signup Page</name>
  <files>app/(auth)/signup/page.tsx</files>
  <behavior>
    - Renders Name, Email, Password, and Confirm Password fields.
    - Validates that fields are not empty.
    - Validates that password is at least 8 characters.
    - Validates that passwords match.
    - Calls /api/auth/register on submission.
    - Displays success message or error message from API.
  </behavior>
  <action>
    Implement the signup page using:
    - shadcn Card, Input, Label, and Button.
    - Card surface color #12121A.
    - "use client" directive.
    - Link to /login.
  </action>
  <verify>
    <automated>grep "/api/auth/register" app/(auth)/signup/page.tsx</automated>
  </verify>
  <done>Signup page implemented with registration logic</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Login Page</name>
  <files>app/(auth)/login/page.tsx</files>
  <behavior>
    - Renders Email and Password fields.
    - Validates that fields are not empty.
    - Calls signIn('credentials', ...) from next-auth/react on submission.
    - Handles query params like 'error' from middleware (email_not_verified, account_disabled).
    - Redirects to /dashboard on success.
  </behavior>
  <action>
    Implement the login page using:
    - shadcn Card, Input, Label, and Button.
    - Card surface color #12121A.
    - "use client" directive.
    - Link to /signup.
  </action>
  <verify>
    <automated>grep "signIn" app/(auth)/login/page.tsx</automated>
  </verify>
  <done>Login page implemented with credentials auth</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User inputs are validated on the client and re-validated on the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | Login/Signup | mitigate | Use POST for submissions; sensitive data (passwords) handled securely |
| T-06-01-02 | Denial of Service | Signup | mitigate | Client-side validation prevents unnecessary API hits; server-side rate limiting (if any) |
</threat_model>

<verification>
- Visit /signup, fill form, check registration.
- Visit /login, enter credentials, check redirect.
- Verify error messages appear for invalid inputs.
</verification>

<success_criteria>
- User can navigate between login and signup.
- Dark theme aesthetic is maintained.
- Successful login redirects to /dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-ui/06-01-SUMMARY.md`
</output>
