---
phase: 06-auth-ui
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified: [
  "services/actionToken.service.ts",
  "app/api/auth/verify/route.ts",
  "app/(auth)/verify/email/page.tsx"
]
autonomous: true
requirements: ["AUTH-01", "AUTH-07"]

must_haves:
  truths:
    - "User can verify email via token link"
    - "System updates isEmailVerified status upon successful verification"
    - "User is redirected to login page after success"
  artifacts:
    - path: "services/actionToken.service.ts"
      provides: "validateToken function"
    - path: "app/api/auth/verify/route.ts"
      provides: "Verification API endpoint"
    - path: "app/(auth)/verify/email/page.tsx"
      provides: "Email verification UI"
  key_links:
    - from: "app/(auth)/verify/email/page.tsx"
      to: "app/api/auth/verify/route.ts"
      via: "fetch in useEffect"
---

<objective>
Implement the email verification page and supporting logic to complete the user registration flow.

Purpose: Allow users to activate their accounts via email link.
Output: validateToken service, verification API route, and verification UI page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/actionToken.service.ts
@app/api/auth/register/route.ts
@app/(auth)/login/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement validateToken in actionToken.service.ts</name>
  <files>services/actionToken.service.ts, __tests__/services/actionToken.service.test.ts</files>
  <behavior>
    - validateToken(userId, token, type) should:
      - Find unconsumed token for user and type.
      - Verify hash matches using bcryptjs.compare.
      - Check if token is expired.
      - Return { success: true } and mark token as consumed if valid.
      - Return { success: false, error: '...' } otherwise.
  </behavior>
  <action>
    Update `services/actionToken.service.ts` to include `validateToken`.
    Ensure it uses `bcryptjs` for comparison (consistent with `createToken`).
    Update tests to verify valid, expired, and incorrect tokens.
  </action>
  <verify>
    <automated>npx vitest run __tests__/services/actionToken.service.test.ts</automated>
  </verify>
  <done>validateToken is implemented and passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create Verification API Route</name>
  <files>app/api/auth/verify/route.ts</files>
  <action>
    Create a POST (or GET) handler in `app/api/auth/verify/route.ts`.
    It should:
    - Accept `token` and `userId` from request body or query.
    - Call `validateToken` from `actionToken.service`.
    - If valid, update `User` record: `isEmailVerified: true`, `emailVerified: new Date()`.
    - Return 200 on success, 400/404 on failure with descriptive error.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/auth/verify -H "Content-Type: application/json" -d '{"token": "invalid", "userId": "some-id"}'</automated>
  </verify>
  <done>API route handles verification requests correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Email Verification Page</name>
  <files>app/(auth)/verify/email/page.tsx</files>
  <action>
    Create a Client Component in `app/(auth)/verify/email/page.tsx`.
    - Extract `token` and `uid` from `useSearchParams`.
    - Show a "Verifying..." loading state on mount.
    - Call `/api/auth/verify` via `fetch`.
    - Handle success: Show success message and provide a button to Login (or auto-redirect).
    - Handle error: Show error message (e.g., "Link expired", "Invalid token").
    - Use the dark theme styling: background `#0A0A0F`, card `#12121A`, consistent with `login/page.tsx`.
  </action>
  <verify>
    <automated>ls app/\(auth\)/verify/email/page.tsx</automated>
  </verify>
  <done>User can navigate to the page and see verification results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Verification token and User ID are untrusted inputs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Verification API | mitigate | Use bcryptjs to verify token hash; ensure token matches user ID; check expiration |
| T-06-04 | Information Disclosure | Verification API | mitigate | Do not leak user existence if ID is invalid; use generic error messages where appropriate |
</threat_model>

<success_criteria>
- validateToken correctly identifies and consumes tokens.
- API route updates user verification status in DB.
- UI provides clear feedback to the user and integrates with the existing Auth UI theme.
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-ui/06-02-SUMMARY.md`
</output>
