---
phase: 03-rebate-engine
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [app/api/cron/process-rebates/route.ts, app/api/admin/trades/upload/route.ts]
autonomous: true
requirements: [REB-05]
user_setup:
  - service: cron
    why: "Scheduled rebate processing"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a secure random string"

must_haves:
  truths:
    - "Rebate processing can be triggered via a secured API endpoint"
    - "Admins can manually upload trade files for processing"
  artifacts:
    - path: "app/api/cron/process-rebates/route.ts"
      provides: "Secured cron trigger"
    - path: "app/api/admin/trades/upload/route.ts"
      provides: "Manual upload endpoint"
  key_links:
    - from: "app/api/cron/process-rebates/route.ts"
      to: "services/rebate.service.ts"
      via: "processBatch"
---

<objective>
Expose the rebate engine via API endpoints for automated (cron) and manual (admin) triggers.
Purpose: To operationalize the rebate engine.
Output: API routes for processing and ingestion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-rebate-engine/03-02-SUMMARY.md
@services/rebate.service.ts
@middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cron API Route</name>
  <files>app/api/cron/process-rebates/route.ts</files>
  <action>
    Implement a POST handler that:
    1. Checks for `Authorization: Bearer {CRON_SECRET}`.
    2. Reads files from a designated landing directory (e.g., `data/pending-trades/`) or accepts a multi-part form upload.
    3. Calls `RebateService.ingestTrades` and `RebateService.processBatch`.
    4. Returns a JSON summary of the run.

    Security: Use `process.env.CRON_SECRET`.
  </action>
  <verify>
    <automated>ls app/api/cron/process-rebates/route.ts</automated>
  </verify>
  <done>Cron endpoint is implemented and secured.</done>
</task>

<task type="auto">
  <name>Task 2: Create Admin Upload API Route</name>
  <files>app/api/admin/trades/upload/route.ts</files>
  <action>
    Implement a POST handler for admin users:
    1. Verify admin session (via middleware or manual check).
    2. Accept file upload (CSV/Excel).
    3. Process the file immediately and return the result summary.

    This endpoint allows admins to manually run the engine without waiting for the cron schedule.
  </action>
  <verify>
    <automated>ls app/api/admin/trades/upload/route.ts</automated>
  </verify>
  <done>Admin upload endpoint is implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| External -> Cron API | Unauthenticated request with secret |
| Admin -> Admin API | Authenticated session |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Summary API | mitigate | Do not return sensitive user PII in the processing summary. |
| T-03-06 | Elevation of Privilege | Admin API | mitigate | Use strict role check (session.user.role === 'ADMIN'). |
</threat_model>

<verification>
- Cron endpoint returns 401 if secret is missing/wrong.
- Admin endpoint returns 401/403 if not logged in as admin.
- Successfully processing a file via API creates ledger entries.
</verification>

<success_criteria>
The system can process a trade log from start to finish via an API call.
</success_criteria>

<output>
After completion, create .planning/phases/03-rebate-engine/03-03-SUMMARY.md
</output>
