---
phase: 03-rebate-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, services/rebate.service.ts]
autonomous: true
requirements: [REB-01, REB-02]

must_haves:
  truths:
    - "Trade data can be stored and tracked for deduplication"
    - "System can parse CSV/Excel files into a normalized format"
    - "MT5 accounts in trade logs are correctly mapped to verified users"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ProcessedTrade model for deduplication"
    - path: "services/rebate.service.ts"
      provides: "Trade ingestion and normalization logic"
  key_links:
    - from: "services/rebate.service.ts"
      to: "xlsx"
      via: "data parsing"
    - from: "services/rebate.service.ts"
      to: "prisma.brokerAccount"
      via: "user mapping"
---

<objective>
Implement the data schema and ingestion service for trade logs.
Purpose: To enable the system to receive and normalize trade data from brokers.
Output: ProcessedTrade model and RebateService ingestion logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@services/ledger.service.ts
@services/broker.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema with ProcessedTrade</name>
  <files>prisma/schema.prisma</files>
  <action>
    Add the ProcessedTrade model to schema.prisma to track individual trades for deduplication.

    Model requirements:
    - id: String (UUID, primary key)
    - tradeId: String (unique, the broker's reference ID)
    - userId: String (relation to User)
    - processedAt: DateTime (default now)

    Add the back-relation to the User model.
    Run `npx prisma generate` after updating.
  </action>
  <verify>
    <automated>grep -q "model ProcessedTrade" prisma/schema.prisma && npx prisma generate</automated>
  </verify>
  <done>ProcessedTrade model exists in schema and client is generated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement RebateService Ingestion</name>
  <files>services/rebate.service.ts</files>
  <action>
    Create RebateService with an `ingestTrades` function.

    Function logic:
    1. Accept a Buffer or file path.
    2. Use `xlsx` to parse the file (CSV or Excel).
    3. Normalize data to a standard format: `{ tradeId, mt5AccountNo, volume, rebatePerLot }`.
    4. Validate that `mt5AccountNo` exists in the system and is VERIFIED/Active.
    5. Return the list of valid, processable trade objects.
  </action>
  <verify>
    <automated>ls services/rebate.service.ts</automated>
  </verify>
  <done>RebateService can parse and normalize trade logs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin -> API | Ingesting external files (Excel/CSV) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | RebateService | mitigate | Validate all fields in trade log before processing; use strict Zod schema. |
| T-03-02 | Denial of Service | RebateService | mitigate | Limit file size and number of rows per batch. |
</threat_model>

<verification>
- Prisma schema includes ProcessedTrade.
- RebateService exports ingestTrades.
- ingestTrades correctly parses a sample CSV.
</verification>

<success_criteria>
System can map a trade log row to a system User via MT5 account number.
</success_criteria>

<output>
After completion, create .planning/phases/03-rebate-engine/03-01-SUMMARY.md
</output>
