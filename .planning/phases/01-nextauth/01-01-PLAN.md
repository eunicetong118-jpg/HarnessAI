---
phase: 01-nextauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, prisma/schema.prisma]
autonomous: true
requirements: [AUTH-NEXTAUTH-SETUP]
must_haves:
  truths:
    - "@auth/prisma-adapter is installed"
    - "Prisma schema contains Account, Session, and VerificationToken models"
    - "Prisma client is regenerated in ../generated/prisma"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "NextAuth standard models"
  key_links:
    - from: "prisma/schema.prisma"
      to: "User model"
      via: "relations to Account and Session"
---

<objective>
Setup the infrastructure and database schema required for NextAuth v5 (Auth.js) using the Prisma adapter.

Purpose: Provide a persistent store for sessions and accounts.
Output: Updated package.json and prisma/schema.prisma.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@prisma/schema.prisma
@package.json
</context>

<tasks>

<task type="auto">
  <name>Install @auth/prisma-adapter</name>
  <files>package.json</files>
  <action>Install the Prisma adapter for Auth.js: `npm install @auth/prisma-adapter`.</action>
  <verify>
    <automated>grep "@auth/prisma-adapter" package.json</automated>
  </verify>
  <done>@auth/prisma-adapter is listed in dependencies</done>
</task>

<task type="auto">
  <name>Update Prisma Schema</name>
  <files>prisma/schema.prisma</files>
  <action>
    Add the following models to `prisma/schema.prisma`:
    - `Account`: Standard NextAuth model for OAuth accounts.
    - `Session`: Standard NextAuth model for database sessions.
    - `VerificationToken`: Standard NextAuth model for verification tokens.

    Update the `User` model:
    - Add `emailVerified DateTime?` (NextAuth standard).
    - Add `image String?`.
    - Add `accounts Account[]`.
    - Add `sessions Session[]`.

    Ensure all models use the appropriate fields as per NextAuth v5 / Auth.js Prisma adapter documentation.
  </action>
  <verify>
    <automated>grep -E "model Account|model Session|model VerificationToken" prisma/schema.prisma</automated>
  </verify>
  <done>Schema updated with NextAuth models and User relations</done>
</task>

<task type="auto">
  <name>Regenerate Prisma Client</name>
  <files>../generated/prisma</files>
  <action>Run `npx prisma generate` to update the Prisma client in the custom output location.</action>
  <verify>
    <automated>test -f ./generated/prisma/models/Account.ts</automated>
  </verify>
  <done>Prisma client regenerated with new models</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB → App | Prisma client provides the data interface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | schema.prisma | mitigate | Ensure sensitive fields like password are not exposed in standard queries (handled in auth service later) |
</threat_model>

<verification>
Run `npx prisma validate` to ensure the schema is correct.
</verification>

<success_criteria>
Prisma client contains the new models and is ready for use in the auth configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-nextauth/01-01-SUMMARY.md`
</output>
