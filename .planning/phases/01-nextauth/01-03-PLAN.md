---
phase: 01-nextauth
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [types/next-auth.d.ts, middleware.ts]
autonomous: true
requirements: [AUTH-NEXTAUTH-TYPES]
must_haves:
  truths:
    - "Session and User types are augmented with role, isDisabled, and isEmailVerified"
    - "Middleware correctly imports and uses the auth function"
  artifacts:
    - path: "types/next-auth.d.ts"
      provides: "Type safety for custom session fields"
---

<objective>
Finalize type safety for NextAuth and verify the middleware integration.

Purpose: Prevent TypeScript errors when accessing custom user fields in sessions.
Output: New types/next-auth.d.ts and verified middleware.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@types/next-auth.d.ts
@middleware.ts
@lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Type Augmentations</name>
  <files>types/next-auth.d.ts</files>
  <action>
    Create `types/next-auth.d.ts` to extend NextAuth types:
    - Augment `next-auth` module.
    - Extend `Session` interface to include `user` with `role`, `isDisabled`, and `isEmailVerified`.
    - Extend `User` interface to include `role`, `isDisabled`, and `isEmailVerified`.
    - Ensure `Role` enum is imported from the generated Prisma client.
  </action>
  <verify>
    <automated>grep -E "interface Session|interface User|isDisabled" types/next-auth.d.ts</automated>
  </verify>
  <done>NextAuth types augmented for custom fields</done>
</task>

<task type="auto">
  <name>Verify Middleware Integration</name>
  <files>middleware.ts</files>
  <action>
    Check `middleware.ts` to ensure it correctly uses the `auth` function from `@/lib/auth`.
    Remove any `@ts-ignore` comments related to user properties once type augmentations are in place and verify that the TypeScript compiler (or IDE) no longer reports errors for `session.user.isDisabled` etc.
  </action>
  <verify>
    <automated>! grep "@ts-ignore" middleware.ts</automated>
  </verify>
  <done>Middleware verified and ts-ignores removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Types → Code | Type definitions ensure runtime access matches expected schema |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Elevation of Privilege | Middleware | mitigate | Middleware must correctly check roles/status; type safety helps ensure correct field usage |
</threat_model>

<verification>
Run `tsc --noEmit` or check for lint errors in `middleware.ts`.
</verification>

<success_criteria>
Project compiles without TypeScript errors related to NextAuth session properties.
</success_criteria>

<output>
After completion, create `.planning/phases/01-nextauth/01-03-SUMMARY.md`
</output>
