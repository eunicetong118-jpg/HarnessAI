---
phase: 01-nextauth
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [lib/auth.ts, app/api/auth/[...nextauth]/route.ts]
autonomous: true
requirements: [AUTH-NEXTAUTH-CONFIG]
must_haves:
  truths:
    - "lib/auth.ts exports auth, handlers, signIn, and signOut"
    - "NextAuth is configured with PrismaAdapter"
    - "API route handler is correctly exported"
  artifacts:
    - path: "lib/auth.ts"
      provides: "NextAuth v5 configuration"
    - path: "app/api/auth/[...nextauth]/route.ts"
      provides: "Auth API endpoints"
---

<objective>
Configure NextAuth v5 logic and setup the API route handlers.

Purpose: Enable authentication flows and session management.
Output: Updated lib/auth.ts and new app/api/auth/[...nextauth]/route.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@lib/auth.ts
@lib/prisma.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Configure lib/auth.ts</name>
  <files>lib/auth.ts</files>
  <action>
    Update `lib/auth.ts` to implement NextAuth v5 configuration:
    - Import `NextAuth` from `next-auth`.
    - Import `PrismaAdapter` from `@auth/prisma-adapter`.
    - Import the prisma client from `@/lib/prisma`.
    - Configure `NextAuth` with:
      - `adapter: PrismaAdapter(prisma)`
      - `session: { strategy: "jwt" }` (Standard for App Router)
      - `providers`: Add a `CredentialsProvider` skeleton that will eventually handle login.
      - `callbacks`:
        - `jwt`: Include `role`, `isDisabled`, and `isEmailVerified` from the user object into the token.
        - `session`: Include the token data into the `session.user` object.
    - Export `{ auth, handlers, signIn, signOut }`.
  </action>
  <verify>
    <automated>grep -E "PrismaAdapter|strategy: \"jwt\"|handlers" lib/auth.ts</automated>
  </verify>
  <done>NextAuth configured and exports available</done>
</task>

<task type="auto">
  <name>Create Auth API Route</name>
  <files>app/api/auth/[...nextauth]/route.ts</files>
  <action>
    Create the file `app/api/auth/[...nextauth]/route.ts` (ensuring the directory exists).
    Export `GET` and `POST` from `handlers` imported from `@/lib/auth`.
  </action>
  <verify>
    <automated>test -f app/api/auth/[...nextauth]/route.ts && grep "GET, POST" app/api/auth/[...nextauth]/route.ts</automated>
  </verify>
  <done>Auth API route established</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Credentials and tokens cross here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | Auth Session | mitigate | Use secure httpOnly cookies and JWT strategy with strong secret |
| T-01-02-02 | Information Disclosure | JWT Token | mitigate | Do not store sensitive info (passwords) in JWT; only public claims (role, status) |
</threat_model>

<verification>
Verify that `lib/auth.ts` compiles without errors.
</verification>

<success_criteria>
Auth handlers are ready and the API route is listening for requests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-nextauth/01-02-SUMMARY.md`
</output>
