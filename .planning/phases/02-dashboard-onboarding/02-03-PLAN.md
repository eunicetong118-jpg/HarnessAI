---
phase: 02-dashboard-onboarding
plan: 03
type: execute
wave: 3
depends_on: ["02-01"]
files_modified: [lib/services/ledger.ts, app/(protected)/dashboard/page.tsx, app/(protected)/dashboard/layout.tsx, components/dashboard/stats-cards.tsx, components/dashboard/rebate-chart.tsx]
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "Dashboard displays Total Earned, Available Balance, and Pending Rebates"
    - "Chart visualizes rebate accumulation over time"
    - "Layout provides consistent navigation for protected routes"
  artifacts:
    - path: "lib/services/ledger.ts"
      provides: "Ledger aggregation logic"
    - path: "app/(protected)/dashboard/page.tsx"
      provides: "Dashboard main view"
    - path: "components/dashboard/rebate-chart.tsx"
      provides: "Recharts visualization"
---

<objective>
Implement the primary User Dashboard with data visualizations.

Purpose: Provide users with a clear overview of their earnings and trading activity.
Output: Dashboard page with stats cards and interactive charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-onboarding/02-RESEARCH.md
@.planning/phases/02-dashboard-onboarding/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LedgerService</name>
  <files>lib/services/ledger.ts</files>
  <action>
    Create `LedgerService` with methods:
    1. `getBalance(userId: string)`: Returns `{ totalEarned: bigint, available: bigint, pending: bigint }`.
       - For now, if no real data exists, it should fall back to mock data or return zeros.
    2. Use Prisma `aggregate` (SUM) for production implementation.

    Note: Handle BigInt to Number conversion for UI (cents to USD).
  </action>
  <verify>
    <automated>npm test __tests__/services/ledger.test.ts</automated>
  </verify>
  <done>Service layer correctly aggregates ledger entries.</done>
</task>

<task type="auto">
  <name>Task 2: Build Dashboard Components</name>
  <files>components/dashboard/stats-cards.tsx, components/dashboard/rebate-chart.tsx</files>
  <action>
    1. Create `StatsCards` component: Displays 3 cards (Total, Available, Pending) using shadcn `Card`.
    2. Create `RebateChart` component: Uses `Recharts` `AreaChart` to display mock accumulation data.
       - Ensure `ResponsiveContainer` is used.
       - Use `ssr: false` or `useEffect` mount check to avoid hydration mismatches.
  </action>
  <verify>
    <automated>ls components/dashboard/*.tsx</automated>
  </verify>
  <done>UI components for dashboard are ready.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble Dashboard Page</name>
  <files>app/(protected)/dashboard/page.tsx, app/(protected)/dashboard/layout.tsx</files>
  <action>
    1. Implement `layout.tsx`: Basic shell with sidebar/topbar navigation.
    2. Implement `page.tsx`:
       - Fetch balances from `LedgerService`.
       - Fetch chart data from `MockDataService`.
       - Render `StatsCards` and `RebateChart`.
  </action>
  <verify>
    <automated>curl -I http://localhost:3000/dashboard</automated>
  </verify>
  <done>Dashboard page displays live/mock data correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service -> Component | Data passed from server to client components (BigInt serialization) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | LedgerService | mitigate | Always filter by `userId` in all database queries. |
</threat_model>

<success_criteria>
- Dashboard renders without hydration errors.
- Currency formatting is correct (e.g., $1,234.56).
- Recharts visualization is responsive and displays correct mock data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-onboarding/02-03-SUMMARY.md`
</output>
