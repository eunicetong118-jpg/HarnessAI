---
phase: 02-dashboard-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [lib/services/broker.ts, app/(protected)/onboarding/page.tsx, lib/actions/broker-actions.ts]
autonomous: true
requirements: [BROK-01, BROK-02]

must_haves:
  truths:
    - "User can select a country and see the corresponding IB registration link"
    - "User can submit an MT5 account number for linkage"
    - "System creates a BrokerAccount record in PENDING state"
    - "System creates a VERIFICATION ticket for admin review"
  artifacts:
    - path: "lib/services/broker.ts"
      provides: "Broker account business logic"
    - path: "app/(protected)/onboarding/page.tsx"
      provides: "Onboarding UI"
    - path: "lib/actions/broker-actions.ts"
      provides: "Server Actions for brokerage"
---

<objective>
Implement the MT5 account linkage workflow (Onboarding).

Purpose: Enable users to link their trading accounts to the platform so rebates can be tracked.
Output: Onboarding page and backend logic for account linkage and ticket creation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-onboarding/02-RESEARCH.md
@.planning/phases/02-dashboard-onboarding/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BrokerService</name>
  <files>lib/services/broker.ts</files>
  <action>
    Create `BrokerService` with a method `linkAccount(userId: string, mt5AccountNo: string)`:
    1. Validate `mt5AccountNo` (numeric, length check).
    2. Check if `mt5AccountNo` is already linked.
    3. Create `BrokerAccount` with `status: PENDING`.
    4. Create a `Ticket` with `type: VERIFICATION` and `status: PENDING`. Store `mt5AccountNo` in metadata.

    Use Prisma transaction to ensure both records are created or none.
  </action>
  <verify>
    <automated>npm test __tests__/services/broker.test.ts</automated>
  </verify>
  <done>Service layer handles account linkage logic robustly.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Onboarding UI</name>
  <files>app/(protected)/onboarding/page.tsx, lib/actions/broker-actions.ts</files>
  <action>
    1. Initialize shadcn components: `card`, `button`, `input`, `select`, `label`.
    2. Implement `/onboarding` page:
       - Step 1: Select Country (using `IB_MAPPING`).
       - Step 2: Show registration link if user hasn't registered via IB.
       - Step 3: Form to enter MT5 Account Number.
    3. Implement Server Action `linkBrokerAccount` in `lib/actions/broker-actions.ts` that calls `BrokerService.linkAccount`.
  </action>
  <verify>
    <automated>curl -I http://localhost:3000/onboarding</automated>
  </verify>
  <done>Onboarding UI is functional and connects to the backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser -> Server Action | Untrusted user input (MT5 Account Number) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Spoofing | linkBrokerAccount | mitigate | Verify `userId` from authenticated session before creating records. |
| T-02-02-02 | Input Validation | BrokerService | mitigate | Use Zod to validate MT5 account number format. |
</threat_model>

<success_criteria>
- User can successfully submit the onboarding form.
- DB records for `BrokerAccount` and `Ticket` are created correctly.
- UI handles loading and error states during submission.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-onboarding/02-02-SUMMARY.md`
</output>
