---
phase: 02-dashboard-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config/ib-mapping.ts, config/milestones.ts, lib/mock-data.ts, __tests__/config.test.ts]
autonomous: true
requirements: [DASH-01, BROK-01]

must_haves:
  truths:
    - "IB mapping config provides correct broker URLs for countries"
    - "Milestones config defines reward thresholds"
    - "Mock data service generates realistic time-series data for Recharts"
  artifacts:
    - path: "config/ib-mapping.ts"
      provides: "Broker IB URL mapping"
    - path: "config/milestones.ts"
      provides: "Reward milestone thresholds"
    - path: "lib/mock-data.ts"
      provides: "Dashboard mock data"
---

<objective>
Setup the foundational configuration and mock data services required for Phase 2 implementation.

Purpose: Isolate static data and provide a predictable source for UI development before the Rebate Engine (Phase 3) is built.
Output: Configuration files and data generation utilities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-onboarding/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Static Configurations</name>
  <files>config/ib-mapping.ts, config/milestones.ts</files>
  <action>
    Create `config/ib-mapping.ts` with an array of objects mapping country codes to IB registration URLs.
    Create `config/milestones.ts` with an array of reward thresholds (e.g., $100, $500, $1000) using BigInt cents.

    Implementation details:
    - `IB_MAPPING` should include at least 3 countries (ID, MY, SG).
    - `MILESTONES` should include values like `10000n` (for $100.00).
  </action>
  <verify>
    <automated>npm test __tests__/config.test.ts</automated>
  </verify>
  <done>Configuration files created and exported correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Mock Data Service</name>
  <files>lib/mock-data.ts</files>
  <action>
    Implement a service in `lib/mock-data.ts` that generates:
    1. `getMockChartData()`: Returns an array of objects for Recharts AreaChart (date, rebate, withdrawn).
    2. `getMockLedgerEntries()`: Returns an array of mock `Ledger` entries.

    Ensure amounts are handled as BigInt where appropriate (cents).
  </action>
  <verify>
    <automated>ts-node -e "import { getMockChartData } from './lib/mock-data'; console.log(getMockChartData())"</automated>
  </verify>
  <done>Mock data generator returns formatted data for components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Config -> App | Static configuration consumed by frontend and backend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | ib-mapping.ts | accept | Config contains public IB URLs; no sensitive data. |
</threat_model>

<success_criteria>
- Configuration files are valid TypeScript and exported.
- Mock data generator produces valid data structures for Recharts.
- Unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-onboarding/02-01-SUMMARY.md`
</output>
