---
phase: 02-dashboard-onboarding
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified: [middleware.ts, app/(protected)/dashboard/layout.tsx, components/dashboard/pending-banner.tsx, components/dashboard/milestone-celebration.tsx]
autonomous: true
requirements: [DASH-03, BROK-02]

must_haves:
  truths:
    - "Users with no linked accounts are redirected to /onboarding"
    - "Pending accounts show an amber warning banner"
    - "Reaching earnings milestones triggers a celebratory animation"
  artifacts:
    - path: "components/dashboard/pending-banner.tsx"
      provides: "Status notification banner"
    - path: "components/dashboard/milestone-celebration.tsx"
      provides: "Lottie animation trigger"
---

<objective>
Finalize the user experience with guards, notifications, and animations.

Purpose: Ensure users follow the correct flow and provide visual feedback for their progress.
Output: Middleware guards, status banners, and celebratory animations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-onboarding/02-RESEARCH.md
@.planning/phases/02-dashboard-onboarding/02-02-SUMMARY.md
@.planning/phases/02-dashboard-onboarding/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Middleware Guards</name>
  <files>middleware.ts</files>
  <action>
    Update `middleware.ts` to include onboarding check:
    1. If user is logged in and accessing `/dashboard`.
    2. Check if user has at least one `BrokerAccount`.
    3. If 0 accounts, redirect to `/onboarding`.
    4. Ensure `/onboarding` is accessible and doesn't cause a redirect loop.

    Note: Use Prisma directly in middleware is not supported in Edge runtime, but since we are using Node.js runtime (per CLAUDE.md/system context), we can use it or better, check a custom session property if we update the session callback.
    Alternatively, perform the check in the layout/page server components for simplicity if middleware complexity is too high.
    Decision: Perform redirect check in `app/(protected)/dashboard/layout.tsx` for reliability.
  </action>
  <verify>
    <automated>curl -L http://localhost:3000/dashboard</automated>
  </verify>
  <done>User flow is correctly enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Status Banner</name>
  <files>components/dashboard/pending-banner.tsx, app/(protected)/dashboard/layout.tsx</files>
  <action>
    1. Create `PendingBanner` component: An amber/yellow banner stating "Account Verification Pending" with an icon.
    2. In `DashboardLayout`, fetch the status of the user's `BrokerAccount`.
    3. Conditionally render the banner at the top of the dashboard if any account is `PENDING`.
  </action>
  <verify>
    <automated>grep "PendingBanner" app/(protected)/dashboard/layout.tsx</automated>
  </verify>
  <done>Users are notified of their account verification status.</done>
</task>

<task type="auto">
  <name>Task 3: Add Milestone Celebrations</name>
  <files>components/dashboard/milestone-celebration.tsx, app/(protected)/dashboard/page.tsx</files>
  <action>
    1. Create `MilestoneCelebration` component using `lottie-react`.
    2. Load a confetti animation (can use a CDN link or local JSON).
    3. Trigger the animation if the user's "Total Earned" balance has crossed a milestone defined in `config/milestones.ts`.
    4. Use local storage or a session flag to ensure the animation only plays once per milestone.
  </action>
  <verify>
    <automated>grep "lottie-react" components/dashboard/milestone-celebration.tsx</automated>
  </verify>
  <done>User achievements are celebrated visually.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server -> Client | Passing account status and balance for UI logic |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | milestone-celebration | accept | Client-side animation trigger is low risk; actual balance is server-verified. |
</threat_model>

<success_criteria>
- Users without accounts are forced to onboarding.
- The pending banner appears correctly for new accounts.
- Milestone animation plays when conditions are met.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-onboarding/02-04-SUMMARY.md`
</output>
