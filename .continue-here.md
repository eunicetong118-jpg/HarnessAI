# Handoff: Rebate Portal V4 Implementation
**Date**: 2026-04-30
**Status**: Completed Engineering Review. Ready for Onboarding UI.

## Progress Summary
- **GStack Upgrade**: v1.4.0.0 → v1.20.0.0 (Browser-skills enabled).
- **Architecture**: Moved `generated/prisma` to `.gitignore` and added `postinstall` generation.
- **Security**: Shortened `ActionToken` length to 16 bytes for better URL compatibility.
- **Performance**: Added `@@index([userId])` to relation models in `schema.prisma`.
- **Optimization**: Narrowed `middleware.ts` matcher to `/dashboard` and `/admin`.
- **Tests**: Added regression test suite for `BrokerService` including MT5 collision checks.
- **Infrastructure**: Verified global `BigInt` serialization polyfill is active.
- **Tracking**: Created `TODOS.md` with P0/P1 priorities.

## Current Context
- **Working Branch**: `main`.
- **Environment**: Next.js 14, Prisma v7, Node.js 25, Bun 1.3.13.
- **State**: Review decisions applied. Local Prisma client regenerated.

## Next Steps
1. **Onboarding Implementation**:
   - Implement `linkAccount` logic in `services/broker.service.ts` (skeleton exists, needs logic).
   - Create `/onboarding` page with country selector mapping to `config/ib-mapping.ts`.
   - Add onboarding redirect logic to middleware/dashboard layout.
2. **Authentication UI**:
   - Implement `LoginForm`, `SignupForm`, and email verification success/error pages.
   - Integrate NextAuth handlers for Credentials provider.

## Decisions
- **BigInt**: Using global `toJSON` polyfill for JSON.stringify support.
- **Middleware**: Targeting protected paths only to skip overhead on public/static routes.
- **MT5 Linkage**: Enforcing uniqueness at the service layer (with regression tests) instead of relying solely on DB errors.

## Resume Instructions
Run `/gsd-resume-work` to pick up from the Onboarding implementation.
